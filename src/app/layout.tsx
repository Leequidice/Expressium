import React from 'react';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Expressium - Minimalist Online Showroom',
  description: 'A curated gallery for displaying creative works, community projects, and artworks in a clean, distraction-free environment.',
  keywords: 'art gallery, creative works, minimalist design, curated exhibition',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
